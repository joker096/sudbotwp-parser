services:
  - type: web
    name: sudbot-parser
    env: node
    region: frankfurt
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: "20"
    autoDeploy: true
    plan: starter
